{
  "name": "tariff-radar",
  "version": "0.2.0",
  "private": true,
  "description": "Static tariff intelligence site with real-source seed data and conservative importer.",
  "scripts": {
    "serve": "python -m http.server 8080",
    "validate": "python scripts/validate_data.py && node --check js/app.js"
  }
}
