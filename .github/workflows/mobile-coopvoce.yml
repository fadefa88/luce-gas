name: mobile · coopvoce

on:
  schedule: [{cron: "29 * * * *"}]
  workflow_dispatch: {}
  push:
    branches: [main]
    paths: ["providers/mobile/coopvoce.py", "lib/**"]

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/_scrape-provider.yml
    with: {module: "providers.mobile.coopvoce", fragment: "mobile__coopvoce"}
