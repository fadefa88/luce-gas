name: mobile · iliad

on:
  workflow_dispatch: {}
  push:
    branches: [main]
    paths: ["providers/mobile/iliad.py", "lib/**"]

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/_scrape-provider.yml
    with: {module: "providers.mobile.iliad", fragment: "mobile__iliad"}
