name: luce · a2a

on:
  schedule: [{cron: "26 * * * *"}]
  workflow_dispatch: {}
  push:
    branches: [main]
    paths: ["providers/luce/a2a.py", "lib/**"]

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/_scrape-provider.yml
    with: {module: "providers.luce.a2a", fragment: "luce__a2a"}
