name: mobile · digi

on:
  workflow_dispatch: {}
  push:
    branches: [main]
    paths: ["providers/mobile/digi.py", "lib/**"]

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/_scrape-provider.yml
    with: {module: "providers.mobile.digi", fragment: "mobile__digi"}
