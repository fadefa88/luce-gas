name: mobile · unomobile

on:
  workflow_dispatch: {}
  push:
    branches: [main]
    paths: ["providers/mobile/unomobile.py", "lib/**"]

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/_scrape-provider.yml
    with: {module: "providers.mobile.unomobile", fragment: "mobile__unomobile"}
