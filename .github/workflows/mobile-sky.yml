name: mobile · sky

on:
  workflow_dispatch: {}
  push:
    branches: [main]
    paths: ["providers/mobile/sky.py", "lib/**"]

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/_scrape-provider.yml
    with: {module: "providers.mobile.sky", fragment: "mobile__sky"}
