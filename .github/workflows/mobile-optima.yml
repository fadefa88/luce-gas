name: mobile · optima

on:
  workflow_dispatch: {}
  push:
    branches: [main]
    paths: ["providers/mobile/optima.py", "lib/**"]

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/_scrape-provider.yml
    with: {module: "providers.mobile.optima", fragment: "mobile__optima"}
