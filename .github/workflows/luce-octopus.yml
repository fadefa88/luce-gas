name: luce · octopus

on:
  schedule: [{cron: "38 * * * *"}]
  workflow_dispatch: {}
  push:
    branches: [main]
    paths: ["providers/luce/octopus.py", "lib/**"]

jobs:
  run:
    uses: ./.github/workflows/_scrape-provider.yml
    with: {module: "providers.luce.octopus", fragment: "luce__octopus"}
