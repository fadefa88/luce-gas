name: mobile · spusu

on:
  workflow_dispatch: {}
  push:
    branches: [main]
    paths: ["providers/mobile/spusu.py", "lib/**"]

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/_scrape-provider.yml
    with: {module: "providers.mobile.spusu", fragment: "mobile__spusu"}
