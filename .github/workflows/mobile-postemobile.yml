name: mobile · postemobile

on:
  workflow_dispatch: {}
  push:
    branches: [main]
    paths: ["providers/mobile/postemobile.py", "lib/**"]

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/_scrape-provider.yml
    with: {module: "providers.mobile.postemobile", fragment: "mobile__postemobile"}
