name: mobile · lycamobile

on:
  workflow_dispatch: {}
  push:
    branches: [main]
    paths: ["providers/mobile/lycamobile.py", "lib/**"]

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/_scrape-provider.yml
    with: {module: "providers.mobile.lycamobile", fragment: "mobile__lycamobile"}
