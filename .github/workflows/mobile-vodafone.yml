name: mobile · vodafone

on:
  workflow_dispatch: {}
  push:
    branches: [main]
    paths: ["providers/mobile/vodafone.py", "lib/**"]

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/_scrape-provider.yml
    with: {module: "providers.mobile.vodafone", fragment: "mobile__vodafone"}
